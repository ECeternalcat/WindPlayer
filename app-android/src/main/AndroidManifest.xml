<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      SEC-4: cleartext traffic is governed by @xml/network_security_config
      (default deny, whitelist 127.0.0.1/localhost for the in-process
      StreamProxy). The legacy `usesCleartextTraffic` attribute is overridden
      by networkSecurityConfig on API 24+ (our minSdk); it's kept here only as
      a documentation hint for older tooling.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="WindPlayer"
        android:supportsRtl="true"
        android:supportsPictureInPicture="true"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.WindPlayer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open video files from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="video/*" />
            </intent-filter>

            <!-- Share video to WindPlayer -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".translate.TranslateService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Whisper AAR requires minSdk 26 but our project targets 24.
             arm64-v8a devices are all API 26+ in practice. -->
        <uses-library
            android:name="com.whispercpp"
            android:required="false"
            tools:node="remove" />
    </application>

    <!-- Override the Whisper AAR's minSdk 26 requirement. -->
    <uses-sdk tools:overrideLibrary="com.whispercpp" />
</manifest>
